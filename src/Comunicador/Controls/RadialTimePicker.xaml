<UserControl x:Class="Comunicador.Controls.RadialTimePicker"
             x:Name="Seletor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="320" Height="320" Focusable="False"
             MouseEnter="Seletor_MouseEnter" MouseLeave="Seletor_MouseLeave">
    <Grid Width="320" Height="320">
        <Ellipse Width="286" Height="286" Fill="{DynamicResource SurfaceBrush}"
                 Stroke="{DynamicResource BorderBrush}" StrokeThickness="1"
                 HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Canvas x:Name="Fatias" Width="320" Height="320" Visibility="Collapsed" />
        <ToggleButton x:Name="HoraAtual" Width="126" Height="56" HorizontalAlignment="Center" VerticalAlignment="Center"
                      Click="HoraAtual_Click" ToolTip="Passe o mouse para ajustar; clique para manter as opções abertas">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Pill" Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="28" Padding="12,5">
                        <TextBlock Text="{Binding Time, ElementName=Seletor}" FontSize="23" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Pill" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /></Trigger>
                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="Pill" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /><Setter TargetName="Pill" Property="BorderThickness" Value="2" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
    </Grid>
</UserControl>
