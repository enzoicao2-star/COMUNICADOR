<UserControl x:Class="Comunicador.Views.HistoricoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <TextBlock Text="Histórico" Style="{StaticResource TituloSecao}" Margin="0" />
            <TextBlock Text="Todas as mensagens enviadas e suas respostas." Style="{StaticResource Subtitulo}" />
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding Filtro, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Filtrar por computador, título ou mensagem..." />
            <Button Grid.Column="2" Style="{StaticResource BotaoSecundario}" Content="Limpar histórico" Command="{Binding LimparCommand}" />
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ItensFiltrados}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Titulo}" FontWeight="SemiBold" />
                                        <TextBlock Style="{StaticResource Subtitulo}" Margin="6,0,0,0">
                                            <Run Text="{Binding Direcao, Converter={StaticResource DirecaoHistoricoToTextoConverter}, Mode=OneWay}" />
                                            <Run Text="{Binding ComputadorNome, Mode=OneWay}" />
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Mensagem}" TextWrapping="Wrap" Margin="0,4,0,4" />
                                    <TextBlock Text="{Binding RespostaTexto, StringFormat='Resposta: {0}'}" TextWrapping="Wrap"
                                               FontStyle="Italic" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,4"
                                               Visibility="{Binding RespostaTexto, Converter={StaticResource TextoParaVisibilidadeConverter}}" />
                                    <TextBlock Text="{Binding Timestamp, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Style="{StaticResource Subtitulo}" />
                                </StackPanel>
                                <Border Grid.Column="1" CornerRadius="10" Padding="10,4" VerticalAlignment="Top"
                                        Background="{Binding Status, Converter={StaticResource StatusEnvioToBrushConverter}}">
                                    <TextBlock Text="{Binding Status, Converter={StaticResource StatusEnvioToTextoConverter}}"
                                               Foreground="White" FontSize="11" FontWeight="SemiBold" />
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
